{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/dockerized-app/package.json",
                "localRoot": "${workspaceFolder}/dockerized-app"
            }
        }
    ]
}